<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.chendemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ChenApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".BasicActivity" />
        <activity android:name=".DialogActivity" />

        <!-- <activity android:name=".HeaderViewPager"></activity> -->
        <activity
            android:name=".launch_mode_a"
            android:launchMode="standard" />
        <activity
            android:name=".launch_mode_b"
            android:launchMode="singleTop" />
        <activity
            android:name=".launch_mode_c"
            android:launchMode="singleTask" />
        <activity
            android:name=".launch_mode_d"
            android:launchMode="singleInstance" />
        <activity android:name=".TimerActivity" />
        <activity android:name=".AnimationActivity"></activity>
    </application>

</manifest>